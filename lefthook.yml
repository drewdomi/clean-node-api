# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://github.com/evilmartians/lefthook/blob/master/docs/configuration.md
#
pre-commit:
  piped: true
  commands:
    format:
      run: pnpm format
    lint:
      run: pnpm lint
    test:
      run: pnpm test:steged
    git:
      run: git add -u .
